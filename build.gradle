subprojects {
    apply plugin: 'java'

    group 'extb.gba'
    version '1.0-SNAPSHOT'

    sourceCompatibility = 1.17


    repositories {
        mavenCentral()
    }

    dependencies {
        testImplementation group: 'junit', name: 'junit', version: '4.12'
    }
}